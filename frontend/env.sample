# Sample environment variables for Writelli frontend
# Copy this file to .env.local for development
# For production, set these variables in your hosting environment

# API URL for backend services
# In production, set this to your actual backend URL
NEXT_PUBLIC_API_URL=https://your-production-api.com

# For local development, this will default to http://localhost:5000
# No need to set it in development unless your local API is on a different port
